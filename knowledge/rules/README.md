# 规则卡

只收两类：① 评审会拍板 ② 预测被验证。每季新增 ≤10 张，失效即删（git 留历史）。
命名 `R-YYYYMM-n.md`，模板见 `templates/rule-card.md`。
agent 答题前必须全量读本目录。

冷启动例外：首批种子卡（R-202608-1~9，含全部9个★级核心方法对应规则）来自历史企划萃取，标「待评审确认」；第一次评审会过一遍：转正（改来源字段）或删。此例外仅此一次。
