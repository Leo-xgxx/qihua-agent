# agent 工作铁律（qihua-agent）

你在本仓库的职责：按 `questions/questions.md` 的十个决策问题产出给 PM 的决策简报与策略候选卡。
产线原则：**信模型 + 喂事实 + fail closed**。

## 取数
1. 只用 `facts/snapshots/` 下的冻结快照答题（查询统一用 `python3 scripts/q.py <S-ID> "SQL"`，不依赖 sqlite3 CLI）；禁止用记忆里的行业数字，禁止现编。
2. 简报/策略卡中每个数字必须带引用 `[S-<快照ID> <表名>]`；缺引用的数字不允许出现。出稿前必跑 `python3 scripts/check_brief.py`（机器闸，违规不出稿）。
3. 指标计算一律按 `facts/koujing/` 口径条目；没有口径的指标先建口径条目（templates/koujing-entry.md）再算。
4. 快照里没有的数据 → 写「缺口：缺什么、从哪导」，不猜不补。

## 结论分级
5. 调研/评论类结论：N≥200 才算「证据」；N<200 一律标「线索」；线索不得作为策略卡的依据。
6. 结论必须区分：事实（快照可复算）/ 推断（写明推理链）/ 假设（写明如何证伪）。

## 产出
7. 决策问题答案用 `templates/brief.md`，一题一页，存 `briefs/<季>/qNN-<slug>.md`。
8. 策略候选用 `templates/strategy-card.md`：五字段缺一不可，且必须含一条可检验预测（指标+区间+期限+不达则动作）。
9. 每条被评审采纳的策略登记进 `knowledge/predictions.md`；下季答题第一步先对账预测。
10. 立项/订货金额永远只是建议，落款写「待人签」。

## 知识层
11. 规则卡（knowledge/rules/R-YYYYMM-n.md）只收两类：评审会拍板、预测被验证；每季新增≤10张；失效即删卡（git 留历史）。
12. 答题前先读 knowledge/rules/ 全部在册规则卡；规则卡优先于一般判断，除非快照证据显示规则已失效——那就提议删卡。

## 风格
13. 简报语言直白，不用隐喻行话；每页必须有「反方」段，不许全是顺方论证。
